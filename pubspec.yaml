name: jira_teko_flutter
description: Auto push testcases and result to jira.
version: 0.1.2
homepage: https://github.com/nghetien/jira_teko_flutter

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
